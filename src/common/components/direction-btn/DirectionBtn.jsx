import React from 'react';
import {Icon} from "../icon/Icon";

export const DirectionBtn = ({id}) => {
    return (
        <Icon sprId={id}
              fill="#000000FF"
              width='50'
              height='50'
              viewBox='0 0 64 64'
        />
    )
}